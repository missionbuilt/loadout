{
  "_comment": "Filled in for any session that doesn't say otherwise, so the daily log stays short. `require` is the metadata every session should carry — log.py names what's missing, and --strict refuses to write without it. Edit this file first: everything here is a placeholder.",
  "_geo": "There is deliberately no location.geo. ingest/weather.py sends those coordinates to Open-Meteo on your first log, and a template that shipped its author's coordinates sent his weather to every stranger who cloned it, silently and correctly-looking. Absent, nothing is sent and log.py says so: `no weather: the session has no coordinates (set location.geo in config/defaults.json)`, and `environment.temp_f` stays on the `require` list below so the gap is named on every run. Add yours as \"geo\": {\"lat\": 39.8, \"lon\": -89.65} inside \"location\" — town level is enough, and it is the only thing here that leaves your machine.",
  "location_detail": "coarse",
  "session": {
    "timezone": "America/New_York"
  },
  "lifter": {
    "_comment": "Facts about the person rather than the session. `sex` selects the DOTS coefficient set and is the one thing no workout log can supply: \"male\" or \"female\". It ships unset on purpose. The two coefficient sets differ by 20-30%, so a guessed score is worse than an absent one - leave this out and no `dots` is written at all, and the Meets card says so rather than showing a number that is not yours.",
    "sex": null
  },
  "location": {
    "name": "Home gym",
    "travel": false
  },
  "program": {
    "_comment": "How `program: next` counts. `cycle` is `weekly` (day N of a `total_days`-day training week, the week number ticks over - the default and what most programs are) or `block` (day N of a `total_days`-day block, no weeks). `meet_date` is optional: set it and every document counts down to it; leave it out and nothing does - most people are not building to a meet. Add \"meet_date\": \"2026-10-24\" here when you have one.",
    "name": "My program",
    "cycle": "weekly",
    "total_days": 4
  },
  "environment": {
    "setting": "garage gym"
  },
  "require": [
    "start_time",
    "duration_min",
    "location",
    "environment.temp_f",
    "metrics.bodyweight_lb",
    "metrics.sleep_hrs"
  ]
}
